const modal=document.getElementById('estimatorModal');const form=document.getElementById('estimateForm');const steps=[...document.querySelectorAll('.form-step')];const progress=document.getElementById('progressBar');let current=1;function showStep(n){current=n;steps.forEach(s=>s.classList.toggle('active',Number(s.dataset.step)===n));progress.style.width=`${n*20}%`;document.querySelector('.modal-card').scrollTop=0}function openEstimator(){modal.classList.add('open');modal.setAttribute('aria-hidden','false');document.body.style.overflow='hidden';showStep(1)}function closeEstimator(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true');document.body.style.overflow=''}function validStep(n){const el=steps.find(s=>Number(s.dataset.step)===n);const fields=[...el.querySelectorAll('input,select')].filter(f=>f.name!=='company');for(const field of fields){if(!field.checkValidity()){field.reportValidity();return false}}return true}document.querySelectorAll('[data-open-estimator]').forEach(b=>b.addEventListener('click',openEstimator));document.querySelectorAll('[data-close-estimator]').forEach(b=>b.addEventListener('click',closeEstimator));document.querySelectorAll('.form-next').forEach(b=>b.addEventListener('click',()=>{if(validStep(current))showStep(Math.min(5,current+1))}));document.querySelectorAll('.form-back').forEach(b=>b.addEventListener('click',()=>showStep(Math.max(1,current-1))));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal.classList.contains('open'))closeEstimator()});function localEstimate(data){const billMap={'under-100':650,'100-175':1050,'176-250':1500,'251-plus':2200};const kwh=billMap[data.billRange]||1000;const low=Math.max(3,Math.round(kwh/145));const high=Math.max(low+1,Math.round(kwh/105));const offset=data.ownership==='own'?'50–85%':'Site dependent';let fit='Needs review';if(data.ownership==='rent')fit='Owner approval needed';else if(data.roofAge==='20+ years')fit='Roof review first';else if(['Asphalt shingle','Metal'].includes(data.roofType))fit='Promising fit';return{systemRange:`${low}–${high} kW`,offsetRange:offset,fitCategory:fit}}form.addEventListener('submit',async e=>{e.preventDefault();if(!validStep(5))return;const button=document.getElementById('submitButton');const error=document.getElementById('formError');button.disabled=true;button.textContent='Saving your estimate…';error.textContent='';const data=Object.fromEntries(new FormData(form).entries());const estimate=localEstimate(data);const payload={...data,estimate,source:location.href,submittedAt:new Date().toISOString()};try{const response=await fetch('/api/submit-lead',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const result=await response.json().catch(()=>({}));if(!response.ok)throw new Error(result.error||'We could not save your request. Please try again.');document.getElementById('resultName').textContent=data.firstName;document.getElementById('systemRange').textContent=estimate.systemRange;document.getElementById('offsetRange').textContent=estimate.offsetRange;document.getElementById('fitCategory').textContent=estimate.fitCategory;form.hidden=true;document.querySelector('.progress').hidden=true;document.getElementById('resultPanel').hidden=false}catch(err){error.textContent=err.message}finally{button.disabled=false;button.textContent='Generate my estimate'}});document.getElementById('year').textContent=new Date().getFullYear();